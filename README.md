# watcher-bots

A collection of small, independent watcher bots.

Each bot monitors external signals (prices, availability, changes)
and sends alerts when predefined conditions are met.

Bots live under `bots/<bot-id>/` and run as scheduled GitHub Actions.
